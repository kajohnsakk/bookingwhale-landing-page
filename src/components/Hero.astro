---

---

<section
  class="relative pt-12 pb-8 lg:pt-16 lg:pb-12 overflow-hidden"
  style="background-color: #fcfcfd;"
>
  <!-- Clean minimal background -->
  <div class="absolute inset-0 overflow-hidden">
    <!-- Subtle grid for structure -->
    <div
      class="absolute inset-0 bg-[url('data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 40 40' width='40' height='40' fill='none' stroke='rgb(156 163 175 / 0.08)'%3e%3cpath d='M40 0v40M0 0v40'/%3e%3c/svg%3e')] opacity-100"
    >
    </div>
  </div>

  <div class="container-tight relative">
    <div
      class="grid lg:grid-cols-12 gap-6 lg:gap-8 items-center min-h-[40vh] lg:min-h-[45vh]"
    >
      <div class="lg:col-span-7 space-y-6 fade-in-up order-2 lg:order-1">
        <div class="space-y-6">
          <div class="relative">
            <h1
              class="text-display text-3xl sm:text-4xl lg:text-5xl xl:text-6xl"
              itemProp="headline"
            >
              <span class="block text-gray-900 leading-tight font-bold">
                <span data-translate="heroTitle">ระบบจองผ่าน</span>
                <span class="line-oa-green relative group inline-block">
                  LINE OA
                  <div
                    class="absolute -bottom-2 left-0 right-0 h-3 bg-green-400/20 -rotate-1 group-hover:rotate-0.5 transition-transform duration-200 ease-out"
                  >
                  </div>
                  <div
                    class="absolute -bottom-2 left-0 right-0 h-3 bg-green-400/10 rotate-1 group-hover:-rotate-0.5 transition-transform duration-200 ease-out"
                  >
                  </div>
                </span>
              </span>
              <span
                class="text-blue-600 block text-xl sm:text-2xl lg:text-3xl xl:text-4xl font-semibold mt-4"
                data-translate="heroSubtitle"
              >
                จองง่าย ขายไว เชื่อมต่อลูกค้าได้ทันที
              </span>
            </h1>
          </div>

          <p
            data-translate="heroDescription"
            class="text-lg lg:text-xl text-gray-600 max-w-2xl leading-relaxed"
          >
            <a href="#services" class="text-blue-600 hover:underline"
              >ระบบจองนัดหมาย</a
            >
            <a href="#features" class="text-blue-600 hover:underline"
              >จัดการคิว</a
            >
            เก็บฐานข้อมูลลูกค้า สร้างระบบจองในแบบของคุณ เชื่อมต่อกับ
            <strong>LINE Official Account</strong> ใช้งานง่าย พร้อมเริ่มได้ทันที
            ดู<a href="#faq" class="text-blue-600 hover:underline"
              >คำถามที่พบบ่อย</a
            >และ
            <a href="#blog" class="text-blue-600 hover:underline">บทความ</a
            >เพื่อข้อมูลเพิ่มเติม
          </p>
        </div>

        <div class="flex flex-col sm:flex-row gap-4 items-center pt-2">
          <button
            onclick="openBookingModal()"
            data-translate="startBookingNow"
            class="group bg-blue-600 hover:bg-blue-700 text-white px-8 py-4 rounded font-bold text-lg shadow-lg hover:shadow-xl transform hover:-translate-y-0.5 hover:scale-102 transition-all duration-150 ease-out flex items-center relative overflow-hidden"
          >
            <!-- Button shimmer effect -->
            <div
              class="absolute inset-0 bg-gradient-to-r from-transparent via-white/10 to-transparent transform -skew-x-12 translate-x-[-100%] group-hover:translate-x-[400%] transition-transform duration-500 ease-out"
            >
            </div>
            <span class="relative z-10">เริ่มใช้ฟรี</span>
            <svg
              class="w-5 h-5 ml-2 group-hover:translate-x-0.5 transition-transform duration-200 ease-out relative z-10"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
            </svg>
          </button>

          <div class="relative group">
            <img
              src="/assets/line-logo.webp"
              alt="LINE Official Account integration for automated booking system - connect instantly with customers through LINE chat"
              class="h-14 opacity-80 hover:opacity-100 transition-all duration-200 ease-out hover:scale-105 transform"
            />
            <!-- Subtle glow effect -->
            <div
              class="absolute inset-0 bg-green-400 rounded-lg opacity-0 group-hover:opacity-8 blur-md transition-opacity duration-250 ease-out"
            >
            </div>
          </div>
        </div>
      </div>

      <!-- Mobile App Mockup -->
      <div
        class="lg:col-span-5 relative fade-in-right order-1 lg:order-2 flex justify-center mt-12 lg:mt-8"
      >
        <!-- Minimal iPhone Frame -->
        <div
          class="relative bg-gray-900 rounded-[2rem] p-1.5 shadow-xl w-64 max-w-xs"
        >
          <!-- iPhone Screen -->
          <div
            class="bg-white rounded-[1.5rem] overflow-hidden h-[480px] relative"
          >
            <!-- Clean Status Bar -->
            <div class="bg-white px-3 py-1.5 flex items-center justify-between">
              <span class="text-xs font-medium text-gray-900">9:41</span>
              <div class="flex items-center space-x-1">
                <div class="w-4 h-2 border border-gray-800 rounded-sm">
                  <div class="w-3 h-1 bg-gray-800 rounded-sm m-0.5"></div>
                </div>
              </div>
            </div>

            <!-- Minimal App Header -->
            <div class="bg-white px-3 py-3 border-b border-gray-100">
              <div class="flex items-center justify-between">
                <div class="flex items-center space-x-2">
                  <img
                    src="/logo.png"
                    alt="BookingWhale - Smart booking management platform logo"
                    class="w-6 h-6 rounded"
                  />
                  <span class="text-sm font-bold text-gray-900"
                    >BookingWhale</span
                  >
                </div>
                <div
                  class="w-6 h-6 bg-gray-900 rounded-full flex items-center justify-center"
                >
                  <span class="text-xs font-bold text-white">A</span>
                </div>
              </div>
            </div>

            <!-- Clean App Content -->
            <div class="px-3 py-3 bg-gray-50 flex-1">
              <!-- Simple Stats -->
              <div class="grid grid-cols-2 gap-2 mb-3">
                <div
                  class="bg-white rounded-lg p-3 text-center border border-gray-200"
                >
                  <div class="text-lg font-bold text-gray-900">42</div>
                  <div class="text-xs text-gray-600">Today's Bookings</div>
                </div>
                <div
                  class="bg-white rounded-lg p-3 text-center border border-gray-200"
                >
                  <div class="text-lg font-bold text-gray-900">320</div>
                  <div class="text-xs text-gray-600">Total Customers</div>
                </div>
              </div>

              <!-- Clean Schedule -->
              <div class="bg-white rounded-lg p-3 mb-3 border border-gray-200">
                <h3 class="text-sm font-bold text-gray-900 mb-2">
                  Today's Queue
                </h3>
                <div class="space-y-2">
                  <div class="flex items-center justify-between">
                    <div class="flex items-center space-x-2">
                      <div
                        class="w-6 h-6 bg-gray-900 rounded-full flex items-center justify-center"
                      >
                        <span class="text-xs font-bold text-white">S</span>
                      </div>
                      <div>
                        <div class="text-xs font-medium text-gray-900">
                          Sarah Johnson
                        </div>
                        <div class="text-xs text-gray-500">13:00 • Haircut</div>
                      </div>
                    </div>
                    <div class="w-2 h-2 bg-green-500 rounded-full"></div>
                  </div>

                  <div class="flex items-center justify-between">
                    <div class="flex items-center space-x-2">
                      <div
                        class="w-6 h-6 bg-gray-900 rounded-full flex items-center justify-center"
                      >
                        <span class="text-xs font-bold text-white">M</span>
                      </div>
                      <div>
                        <div class="text-xs font-medium text-gray-900">
                          Mike Chen
                        </div>
                        <div class="text-xs text-gray-500">14:30 • Massage</div>
                      </div>
                    </div>
                    <div class="w-2 h-2 bg-blue-500 rounded-full"></div>
                  </div>

                  <div class="flex items-center justify-between">
                    <div class="flex items-center space-x-2">
                      <div
                        class="w-6 h-6 bg-gray-900 rounded-full flex items-center justify-center"
                      >
                        <span class="text-xs font-bold text-white">L</span>
                      </div>
                      <div>
                        <div class="text-xs font-medium text-gray-900">
                          Lisa Williams
                        </div>
                        <div class="text-xs text-gray-500">
                          16:00 • Manicure
                        </div>
                      </div>
                    </div>
                    <div class="w-2 h-2 bg-yellow-500 rounded-full"></div>
                  </div>
                </div>
              </div>

              <!-- Simple Actions -->
              <div class="grid grid-cols-2 gap-2">
                <button class="bg-gray-900 rounded-lg p-3 text-center">
                  <div class="text-xs font-bold text-white">New Booking</div>
                </button>
                <button
                  class="bg-white rounded-lg p-3 text-center border border-gray-200"
                >
                  <div class="text-xs font-bold text-gray-900">View Stats</div>
                </button>
              </div>
            </div>

            <!-- Clean Bottom Nav -->
            <div class="bg-white px-3 py-2 border-t border-gray-200">
              <div class="flex justify-around">
                <div class="flex flex-col items-center py-1">
                  <div
                    class="w-5 h-5 bg-gray-900 rounded flex items-center justify-center"
                  >
                    <div class="w-2 h-2 bg-white rounded"></div>
                  </div>
                  <span class="text-xs text-gray-900 mt-1 font-medium"
                    >หน้าหลัก</span
                  >
                </div>
                <div class="flex flex-col items-center py-1">
                  <div
                    class="w-5 h-5 bg-gray-300 rounded flex items-center justify-center"
                  >
                    <div class="w-2 h-2 bg-gray-600 rounded"></div>
                  </div>
                  <span class="text-xs text-gray-500 mt-1">ปฏิทิน</span>
                </div>
                <div class="flex flex-col items-center py-1">
                  <div
                    class="w-5 h-5 bg-gray-300 rounded flex items-center justify-center"
                  >
                    <div class="w-2 h-2 bg-gray-600 rounded"></div>
                  </div>
                  <span class="text-xs text-gray-500 mt-1">ตั้งค่า</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Enhanced animations for engaging hero section */
  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes fade-in-right {
    from {
      opacity: 0;
      transform: translateX(30px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes float-slow {
    0%,
    100% {
      transform: translateY(0px) rotate(0deg);
    }
    50% {
      transform: translateY(-15px) rotate(5deg);
    }
  }

  @keyframes float-delayed {
    0%,
    100% {
      transform: translateY(0px) translateX(0px);
    }
    33% {
      transform: translateY(-10px) translateX(5px);
    }
    66% {
      transform: translateY(-5px) translateX(-3px);
    }
  }

  @keyframes float-gentle {
    0%,
    100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-8px);
    }
  }

  @keyframes bounce-gentle {
    0%,
    100% {
      transform: translateY(0px) scale(1);
    }
    50% {
      transform: translateY(-10px) scale(1.05);
    }
  }

  @keyframes pulse-gentle {
    0%,
    100% {
      opacity: 0.5;
      transform: scale(1);
    }
    50% {
      opacity: 0.8;
      transform: scale(1.1);
    }
  }

  @keyframes rotate-slow {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  @keyframes card-hover {
    0%,
    100% {
      transform: translateY(0px) rotate(1deg);
    }
    50% {
      transform: translateY(-5px) rotate(0.5deg);
    }
  }

  /* Modern Minimal Animation Classes */
  .fade-in-up {
    animation: fade-in-up 0.8s ease-out;
  }

  .fade-in-right {
    animation: fade-in-right 0.8s ease-out 0.2s both;
  }

  .animate-float-up {
    animation: float-up 6s infinite ease-in-out;
  }

  .animate-scale-breathe {
    animation: scale-breathe 4s infinite ease-in-out;
  }

  .animate-rotate-gentle {
    animation: rotate-gentle 8s infinite linear;
  }

  .animate-slide-x {
    animation: slide-x 5s infinite ease-in-out;
  }

  .animate-float-delayed {
    animation: float-delayed 6s infinite ease-in-out 1s;
  }

  .animate-bounce-gentle {
    animation: bounce-gentle 3s infinite ease-in-out;
  }

  /* Clean Minimal Keyframes */
  @keyframes float-up {
    0%,
    100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-8px);
    }
  }

  @keyframes scale-breathe {
    0%,
    100% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.1);
    }
  }

  @keyframes rotate-gentle {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  @keyframes slide-x {
    0%,
    100% {
      transform: translateX(0px);
    }
    50% {
      transform: translateX(10px);
    }
  }

  @keyframes bounce-gentle {
    0%,
    100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-4px);
    }
  }

  /* Performance optimizations */
  .animate-float-slow,
  .animate-float-delayed,
  .animate-float-gentle,
  .animate-bounce-gentle,
  .animate-pulse-gentle,
  .animate-card-hover {
    will-change: transform;
  }

  /* Mobile responsive adjustments */
  @media (max-width: 640px) {
    .hero-visual {
      padding: 1rem;
    }

    .secondary-card {
      transform: scale(0.8);
    }

    /* Reduce animation intensity on mobile */
    .animate-float-slow,
    .animate-float-delayed,
    .animate-rotate-slow {
      animation-duration: 8s;
    }
  }

  /* Reduced motion support */
  @media (prefers-reduced-motion: reduce) {
    .fade-in-up,
    .fade-in-right,
    .animate-float-slow,
    .animate-float-delayed,
    .animate-float-gentle,
    .animate-bounce-gentle,
    .animate-pulse-gentle,
    .animate-rotate-slow,
    .animate-card-hover {
      animation: none;
    }
  }

  /* Custom shadow for professional look */
  .shadow-3xl {
    box-shadow:
      0 35px 60px -12px rgba(0, 0, 0, 0.25),
      0 20px 25px -5px rgba(0, 0, 0, 0.1);
  }

  /* LINE OA Green color */
  .line-oa-green {
    color: #06d755 !important;
    font-weight: bold !important;
  }
</style>
